{
  "//": "Lint-only project so the type-aware ESLint rules see the same files `npm run typecheck` does. It extends tsconfig.tests.json (tests + e2e + the two config files) rather than the client config, so a rule and the compiler can never disagree about what a test file's types are.",
  "extends": "./tsconfig.tests.json"
}
